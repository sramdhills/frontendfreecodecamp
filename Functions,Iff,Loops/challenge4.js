function makeNegative(num) {
    // Code?
    num = -42
    if (num < 0)
        return num
    else
        return num * -1
    
    
  }
  console.log(makeNegative())
//absolute value
  function makeNegativeabs(num1){
    return -Math.abs(num);
  }