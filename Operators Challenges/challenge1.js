const numberVariable = 0;

// numberVariable++
// numberVariable++
// numberVariable++
//it didn't work because the variable is constant.Thus cannot be changed.

console.log(numberVariable);

//now it works!